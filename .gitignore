# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / build
build/
dist/
*.egg-info/
.eggs/
*.egg
wheels/
pip-wheel-metadata/

# === Virtual environments ===
.venv/
venv/
env/
ENV/
.python-version

# === Local-only seeded engine config (generated by `engine.cli seed-config`) ===
engine_config.local.yaml

# === Test / coverage caches ===
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache/
coverage.xml
*.cover

# === Type checkers / linters ===
.mypy_cache/
.dmypy.json
.ruff_cache/
.pyre/

# === Local run artifacts (generated during local testing) ===
# The delivered config/engine_config.yaml skeleton is tracked; the config
# produced by `engine.cli seed-config` is reproducible, so it's ignored.
config/engine_config_seeded.yaml
*_seeded.yaml
scratch_server.log
*.log

# === OS / editor junk ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# === Secrets / local env ===
.env
.env.*
!.env.example
